# Kimi Browser QA

- Last checked: 2026-07-13
- Result: `blocked-auth`
- Page result: the public composer loaded, but submitting the redacted QA prompt still required login instead of producing a policy-verifiable conversation turn.
- Production-protocol result: the current public bundle exposes paged `ChatService/ListMessages` history with stable message IDs, `parent_id` links, Vue segment identities, and a native previous-segment loader. TurnMap's implementation captures and paginates that structured history without scrolling.
- Extension result: the existing anonymous smoke run injected exactly one TurnMap launcher. Synthetic coverage now verifies paged/off-screen indexing, repeated prompts, multi-assistant segment merge, remount-stable IDs, SPA cache isolation, exact target validation, and unchanged extraction scroll behavior.
- Capability decision: keep the registry at `blocked-auth` / mounted-DOM. Native indexing and exact-ID navigation are implemented, but no authenticated 20-turn conversation proves real-browser completeness or off-screen revival yet.

Evidence: `screenshots/kimi-extension-baseline.png`, `screenshots/kimi-anonymous-attempt.png`.
