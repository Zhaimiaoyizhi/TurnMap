# Claude Browser QA

- Date: 2026-07-12
- Result: `blocked-access`
- Page result: Cloudflare security verification prevented access to a conversation.
- Extension result: the content script still injected exactly one TurnMap launcher on the challenge page.
- Capability decision: keep mounted-DOM extraction and mounted-only exact jump; challenge-page injection is not conversation support evidence.

Evidence: `screenshots/claude-auth-baseline.png`, `screenshots/claude-extension-baseline.png`.

## 2026-07-13 Implementation Update

- Browser result: a fresh isolated browser session again stopped at Cloudflare security verification; no challenge was bypassed and no conversation evidence is claimed.
- Protocol evidence: the existing read-only research fixture identifies Claude's conversation-detail response under `/api/organizations/<organization>/chat_conversations/<conversation>?tree=True&rendering_mode=messages&render_all_tools=true`. TurnMap now observes only the page's already-occurring GET response; it does not issue an additional request.
- Implementation: a clean-room MAIN-world observer buffers the eager response until the isolated content script is ready. The Claude index uses conversation, user-message, assistant-message, parent-message, and selected-leaf UUIDs to preserve repeated prompts and active edit/retry branches without extraction scrolling.
- Navigation: exact mounted UUID targets are revealed directly. A fully mounted, exact ordered branch can be rebound after DOM remount. Partial virtual windows are not matched by text or relative position, and an unmounted target fails explicitly because no verified Claude-native message route or deterministic virtual-item controller is available.
- Automated evidence: `tests/claude-native-navigation.test.mjs` covers full structured indexing, repeated text, branch selection, explicit deletion tombstones, grow-only partial captures, SPA isolation, exact remount binding, streaming suffixes, safe failure, adapter routing, and passive observer installation.
- Capability decision: retain `blocked-access`, `mounted-dom`, and `mounted-only`. The structured path is implemented but cannot be promoted without authenticated long-conversation and early off-screen acceptance evidence.
