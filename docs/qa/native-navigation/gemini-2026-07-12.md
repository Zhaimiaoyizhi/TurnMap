# Gemini Browser QA

- Date: 2026-07-12
- Result: `smoke-verified` at the mounted-DOM tier
- Page result: an anonymous session completed two consecutive prompts with identical text and kept both user turns and answers visible.
- Extension result: exactly one launcher was injected; the side panel mapped two distinct nodes, proving repeated prompt text was not collapsed.
- Regression found: the second streaming answer initially remained `No text response`. The shared merge/storage defect was fixed and browser-verified on the same shared path with Qwen.
- Capability decision: mounted repeated-turn identity is verified. Full native indexing, distant off-screen jump, and shell remount remain unverified.

Evidence: `screenshots/gemini-repeated-prompt-smoke.png`, `screenshots/gemini-extension-baseline.png`, `tests/turn-merge.test.mjs`, `tests/graph-storage.test.mjs`.
