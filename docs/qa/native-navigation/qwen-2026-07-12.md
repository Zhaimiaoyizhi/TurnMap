# Qwen Browser QA

- Date: 2026-07-12
- Result: `smoke-verified` at the mounted-DOM tier
- Page result: an anonymous session completed two consecutive prompts with identical text and two visible assistant answers.
- Extension result: exactly one launcher was injected; the side panel mapped two distinct turns. After the streaming-enrichment fixes, a refresh displayed the completed answer on both nodes.
- Regression found and fixed: a streamed `No text response` placeholder was preserved by side-panel merging and by persisted default node text. Tests now cover both layers.
- Capability decision: repeated mounted identities and incremental answer enrichment are browser-verified. Full native indexing, distant off-screen jump, and shell remount are not verified, so the adapter remains mounted-DOM/mounted-only.

Evidence: `screenshots/qwen-repeated-prompt-smoke.png`, `screenshots/qwen-stream-enrichment-fixed.png`, `tests/turn-merge.test.mjs`, `tests/graph-storage.test.mjs`.
